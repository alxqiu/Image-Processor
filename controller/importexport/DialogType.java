package controller.importexport;

/**
 * Enum that contains all current types of dialog boxes needed for the
 * import/export operations to succeed on the swing view.
 */
public enum DialogType {
  OPEN, SAVE;
}
