package controller;

/**
 * Interface defining behavior for a controller for Image processing operations.
 */
public interface IController {
  /**
   * Starts the control loop going for the controller, or begins the program.
   */
  void startProcessing();
}
